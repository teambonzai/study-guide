# --- Raw lecture originals: never publish (copyright) or commit (size) ---
src/
**/source-materials/

# --- Intermediate slide PNGs: already base64-embedded in each index.html ---
**/assets/

# --- Per-slide text JSON: already embedded as `slidedata` in each index.html ---
**/slides.json

# --- Cruft ---
.DS_Store
*.zip

# Python bytecode
__pycache__/
*.pyc
